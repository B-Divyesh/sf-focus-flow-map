[
  {
    "id": "demo-isolated",
    "claim": "Try a six-step sample focus route without reading or changing real license data",
    "where": "landing first screen, demo banner, README",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:demo-isolated",
    "sandbox": "fresh browser context; enter /?demo=1 from the first-screen action; instrument every localStorage operation and outgoing request; reset and leave the demo"
  },
  {
    "id": "keyboard-demo",
    "claim": "Operate the sample report and demo controls with the keyboard",
    "where": "demo controls",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:keyboard-demo",
    "sandbox": "fresh browser context at /?demo=1; use Tab and Enter only; assert focus and visible state changes"
  },
  {
    "id": "offline-sample-route",
    "claim": "After one online visit, the sample route and its review controls remain available offline",
    "where": "landing offline license status, README, .factory/demo.md",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:offline-sample-route",
    "sandbox": "fresh isolated browser context with service workers enabled; visit online once, trigger the offline license state, open and reload the cached demo offline, then operate review notes and Reset demo"
  },
  {
    "id": "mobile-first-view",
    "claim": "On a 390 px phone, the first screen shows all three product facts and the demo shows a sample focus-route row after one click",
    "where": "landing first screen, demo workspace",
    "test": "npm run test:e2e -- --project=mobile-390 --grep @claim:mobile-first-view",
    "sandbox": "fresh 390×844 browser context; open home, assert all facts appear on the first screen, click the sample action, and assert the first focus-route row appears on the first screen"
  },
  {
    "id": "chromium-package",
    "claim": "Download a packaged Chromium extension from the first screen",
    "where": "landing first screen, installation steps, README",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:chromium-package",
    "sandbox": "fresh browser context; follow the first-screen package URL; assert a successful non-empty ZIP response"
  },
  {
    "id": "explicit-recording",
    "claim": "Recording begins only after the user chooses to start",
    "where": "landing facts, workflow, README, privacy policy",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:explicit-recording",
    "sandbox": "fresh Chromium extension profile on the /?demo=1 sample; inspect extension storage and the page before sending START_RECORDING"
  },
  {
    "id": "local-session-privacy",
    "claim": "Focus sessions stay in extension storage and no audit content is sent to a server",
    "where": "landing privacy section, README, privacy policy",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:local-session-privacy",
    "sandbox": "fresh Chromium extension profile on the /?demo=1 sample; capture all requests and inspect chrome.storage.local after recording"
  },
  {
    "id": "license-request-minimum-data",
    "claim": "License verification sends only the license token to Sociobot",
    "where": "landing privacy section, privacy policy",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:license-request-minimum-data",
    "sandbox": "fresh browser context with a stale fixture license; intercept verification; record method, URL, headers, and body; assert one token query value and no request body"
  },
  {
    "id": "sensitive-redaction",
    "claim": "Reports exclude query strings, hashes, input values, and sensitive URL path values",
    "where": "landing privacy section, README, privacy policy, report export panel",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:sensitive-redaction",
    "sandbox": "fresh extension profile; demo URL contains an encoded email, query, and fragment; a synthetic input contains a secret fixture; inspect storage and both exports"
  },
  {
    "id": "markdown-json-export",
    "claim": "Export the recorded route as Markdown and JSON for free",
    "where": "landing first-screen facts, workflow, pricing, README",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:markdown-json-export",
    "sandbox": "fresh free extension profile; record the /?demo=1 sample; download and inspect both report formats"
  },
  {
    "id": "route-checks",
    "claim": "Raise review notes for page jumps, hidden targets, repeats, stalls, and missing focus indicators",
    "where": "landing workflow, README, report map",
    "test": "npm run test:unit -- --testNamePattern @claim:route-checks",
    "sandbox": "recorded focus-session fixture containing each route condition; assert every check kind"
  },
  {
    "id": "recorded-route-fields",
    "claim": "A recorded focus route includes order, direction, safe selectors, labels, page position, page movement, visibility, and focus indicators",
    "where": "README What v1 does, landing workflow",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:recorded-route-fields",
    "sandbox": "fresh Chromium extension profile; record controls on the demo entry point; inspect every stored step and assert every documented field and safe selector shape"
  },
  {
    "id": "history-limits",
    "claim": "Free mode keeps the latest local session and Pro keeps up to 30",
    "where": "landing pricing, README, extension report",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:history-limits",
    "sandbox": "fresh extension profile; save two free fixture sessions, then 31 locally licensed fixture sessions; inspect chrome.storage.local"
  },
  {
    "id": "pro-local-notes",
    "claim": "Pro saves private audit notes on the current device",
    "where": "landing pricing, README, extension report",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:pro-local-notes",
    "sandbox": "fresh extension profile with a recorded valid-license fixture; save a note and inspect extension-origin localStorage"
  },
  {
    "id": "pro-unavailable",
    "claim": "Pro license sales are unavailable and no checkout action is offered",
    "where": "landing Pro status, README, terms, extension report",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:pro-unavailable",
    "sandbox": "fresh site and packaged extension contexts; assert the unavailable state, absence of every checkout link or purchase action, and continued access to free exports"
  },
  {
    "id": "original-image-provenance",
    "claim": "The original hero illustration was generated for Focus Flow Map with the factory image model",
    "where": "landing hero caption, landing footer",
    "test": "npm run test:unit -- --testNamePattern @claim:original-image-provenance",
    "sandbox": "repository checkout; inspect the dated local provenance record, prompt, accepted review, source image, shipped derivative, and visual thesis disclosure"
  },
  {
    "id": "no-third-party-runtime",
    "claim": "The site uses no analytics, external runtime scripts, CDN fonts, or remote session storage",
    "where": "README Architecture, privacy policy",
    "test": "npm run test:e2e -- --project=desktop-chromium --grep @claim:no-third-party-runtime",
    "sandbox": "fresh isolated browser context with service workers blocked; capture home and demo requests and resource types; assert same-origin scripts and fonts, no frames, and isolated demo storage"
  }
]
